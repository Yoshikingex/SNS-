# ===== apps/web =====
# Next.js 環境変数の雛形（値はすべて [REDACTED]）
NEXT_PUBLIC_APP_URL=[REDACTED]

# Supabase（Phase2 で利用予定。現時点ではプレースホルダ）
NEXT_PUBLIC_SUPABASE_URL=[REDACTED]
NEXT_PUBLIC_SUPABASE_ANON_KEY=[REDACTED]
SUPABASE_SERVICE_ROLE_KEY=[REDACTED]

# ===== apps/extension =====
# 拡張で使用予定の値（Phase以降。現時点ではプレースホルダ）
VITE_PUBLIC_API_BASE=[REDACTED]

# ===== packages/shared (暗号化) =====
# AES-256-GCM 用の32バイト鍵（Base64エンコード）
# 生成例: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Phase 3 以降で apps/web のサーバ側から使用予定。値は [REDACTED]
ENCRYPTION_KEY=[REDACTED]
